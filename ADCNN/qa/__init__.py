"""QA / diagnostic figure generators for the ADCNN pipelines.

  * :mod:`ADCNN.qa.plots_runtime`     -- ``run_night`` wall-time-per-stage/visit/detector figures.
  * ``plots_thresholds`` (added with the module reorg) -- the threshold-selection figures, which
    import the canonical curves from :mod:`ADCNN.calibration.threshold_selection`.
"""
