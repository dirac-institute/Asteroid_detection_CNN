"""Same-night track linking + alert ranking.

  * :mod:`ADCNN.linking.link_2visit` -- the trail-state same-night linker (chord-seeded 2-visit +
    3+visit tiers; exact orbit-fit chi2 + physical_check gates).
  * :mod:`ADCNN.linking.rank_alerts` -- the ranked same-night alert stream (priorityScore packets).
"""
