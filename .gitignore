# Python bytecode
__pycache__/
*.pyc
*.pyo
*.pyd

# Claude Code local state
.claude/

# Editors
.idea/
.vscode/

# macOS
.DS_Store

# Jupyter Notebook checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Build / distribution
build/
dist/
*.egg-info/

# Virtual environments
venv/
.env/

# Project data (raw inputs + built diffim datasets)
DATA/
DATA_DIFFIM/
*.h5
*.npz

# Training run outputs (checkpoints, logs, scratch artefacts)
runs/
checkpoints/
plots/
*.log

# Vendored HelioLinC3D build (built from source, not redistributed)
ADCNN/pipelines/heliolinc/heliolinc2/
external/

# ALL runtime outputs live under repo-root outputs/ (runs/, logs/, training_runs/,
# query_snapshots/, attic/). Layout since 2026-07-14; nothing writes into the
# package tree at runtime.
outputs/

# Defensive: legacy output locations -- catch anything an old script/checkout still emits
ADCNN/pipelines/heliolinc/run_*/
ADCNN/pipelines/heliolinc/NEO_*/
ADCNN/pipelines/heliolinc/fp_budget*/
ADCNN/pipelines/heliolinc/rejecter_data/
ADCNN/pipelines/heliolinc/rb_*.csv
ADCNN/pipelines/heliolinc/cadence*.csv
ADCNN/pipelines/heliolinc/field_*.csv
Evaluation/catalogs_thr0/
Evaluation/tmp_stream_*/

# User scratch + nested unrelated checkouts (LSST tutorial-notebooks etc.)
mrakovci/
mrakovci
